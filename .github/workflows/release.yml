# SPDX-License-Identifier: MIT
---
name: release-please

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - run: echo "release automation is paused."
